# Minimal redirects for Cloudflare Workers
# Only essential rules, no external URLs

/* / 301

/home / 301

/sitemap.xml /sitemap.xml 200

/robots.txt /robots.txt 200

/.well-known/* /.well-known/:splat 200